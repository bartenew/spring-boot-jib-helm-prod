apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "haiku.fullname" . }}-test-connection"
  labels:
    {{- include "haiku.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "haiku.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
